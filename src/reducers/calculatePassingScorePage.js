import * as R from 'ramda'

import selectSpecialityPageActions from 'actions/selectSpecialityPage'

const initialState = {
}

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
